/* Adds two fractions */
# include <stdio.h>
int main(void){
	int m,d,y;
	printf("Enter a data(mm/dd/yyyy): ");
	scanf("%d/%d/%d", &m, &d, &y);
	// printf("You entered the date %d%d%d", y,m,d);
	print("%d[0]", m);
	return 0;
}