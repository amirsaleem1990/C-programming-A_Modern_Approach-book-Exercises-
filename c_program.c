/* Adds two fractions */
# include <stdio.h>
int main(void){
	int item_numer, month, day, year;
	float unit_price;
	printf("Enter item number: ");
	scanf("%d", &item_numer);
	printf("Enter unit price: ");
	scanf("%f", &unit_price);
	printf("Enter purchse date(mm/dd/yyyy): ");
	scanf("%d%d%d", &month, &day, &year);
	printf("%20s", "amir saleem");
	return 0;
}